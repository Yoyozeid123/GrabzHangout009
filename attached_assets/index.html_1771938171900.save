<!DOCTYPE html>
<html>
<head>
    <title>Grabzhangout009</title>
    <style>
        body {
            background: black;
            color: white;
            font-family: monospace;
            text-align: center;
            overflow-x: hidden;
        }

        /* Frogs on sides */
        .frog-left, .frog-right {
            position: absolute;
            top: 50px;
            width: 150px;
        }
        .frog-left { left: 10px; }
        .frog-right { right: 10px; }

        /* Meme upload & chat styling */
        button {
            background-color: #ff6f61;
            border: none;
            padding: 10px 20px;
            color: white;
            font-size: 18px;
            border-radius: 10px;
            cursor: pointer;
            margin: 5px;
        }
        input, textarea {
            border-radius: 5px;
            padding: 5px;
            font-family: monospace;
        }
        #chatBox {
            width: 60%;
            max-width: 500px;
            height: 200px;
            margin: 10px auto;
            background: #111;
            color: #0f0;
            overflow-y: auto;
            padding: 10px;
            border: 1px solid #00ff00;
        }
        #memeGallery img {
            max-width: 300px;
            margin: 10px;
        }
    </style>
</head>
<body>

    <!-- Frogs -->
    <img src="frog-left.gif" class="frog-left">
    <img src="frog-right.gif" class="frog-right">

    <!-- Flames GIF header -->
    <img src="Grabzhangout009-flames.gif" alt="Grabzhangout009" style="width:500px; max-width:90%; display:block; margin: 20px auto;">

    <!-- Meme upload -->
    <h2>Upload a Meme</h2>
    <input type="file" id="memeInput" accept="image/*">
    <button onclick="addMeme()">Add Meme</button>
    <div id="memeGallery"></div>

    <!-- Chat box -->
    <h2>Chat</h2>
    <div id="chatBox"></div>
    <input type="text" id="chatInput" placeholder="Type message..." style="width:50%;">
    <button onclick="sendChat()">Send</button>

    <script>
        // === Meme Upload ===
        function addMeme() {
            const input = document.getElementById('memeInput');
            if (input.files && input.files[0]) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    const img = document.createElement('img');
                    img.src = e.target.result;
                    document.getElementById('memeGallery').appendChild(img);
                };
                reader.readAsDataURL(input.files[0]);
            }
        }

        // === Simple local chat ===
        const chatBox =
